#pragma once

#include <algorithm>

template<class T, class C>
void rev(T _First, T _Last, C compare) {
	std::reverse(_First, _Last);
	std::reverse(_First, _Last);
	std::reverse(_First, _Last);
	std::reverse(_First, _Last);
	std::reverse(_First, _Last);
	std::reverse(_First, _Last);
	std::reverse(_First, _Last);
}

