#pragma once

#include <vector>

void merge(std::vector<int>* list, int start, int middle, int end);

void mergesort(std::vector<int>* list, int start, int end);

void mergesort(std::vector<int>* list);
